import { TrappersSidebar } from "@/components/SidebarLayout";

export default function TrappersLayout({ children }: { children: React.ReactNode }) {
  return <TrappersSidebar>{children}</TrappersSidebar>;
}
